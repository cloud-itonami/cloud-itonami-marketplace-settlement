{
  "private": true,
  "scripts": {
    "build": "shadow-cljs release worker",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@noble/hashes": "^2.2.0",
    "shadow-cljs": "^2.28.20",
    "wrangler": "^4.0.0",
    "@noble/curves": "^1.6.0",
    "@ipld/dag-cbor": "^9.2.1"
  },
  "name": "cloud-itonami-marketplace-settlement",
  "comment": "shadow-cljs compiles src/settleops/edge/worker.cljs -> dist/worker.js."
}
